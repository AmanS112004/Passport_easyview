# The base URL of your backend API
# For local development, leave it as /api if using Vite proxy
# For production (Netlify), set it to your Render backend URL (e.g., https://your-api.onrender.com/api)
VITE_API_URL=/api
